"""tuples are similar to list.However they have one key difference -immutability (cannot change)
once an element is inside a tuple ,it cannot be reassigned
tuples use (1,2,3)"""

t = (1,3,4,5)
print(t)
print(type(t))
print(len(t))
print(t[0])

print(t.count(1))
print(t.index(3))
print(t.index(4))

